This is my solution to the basic tasks.

Some notes:
  1. When a patient has more than one given name, those names are represented by a single string, diveded by ",". that string is stored in an array.
     exp: [ "John\",\"Joe" ] instead of ["John" , "Joe"]
